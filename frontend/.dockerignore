# Ignore local node_modules and package manager stores
node_modules
.pnpm-store

# Ignore Next/Build outputs (CRITICAL: evita conflictos de caché)
.next
out
build
dist

# Ignore git and editor files
.git
.gitignore
.vscode
.idea

# Environment and secrets
.env
.env.local
.env.*.local

# System files
.DS_Store
Thumbs.db

# Logs and temp files
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
coverage

# Docker files (don't ignore Dockerfile itself if building from this folder)
# keep Dockerfile in context

# Others
.pnp.*
