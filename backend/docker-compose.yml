services:
  backend:
    build:
      context: .
      target: production
    container_name: portfolio-backend-prod
    ports:
      - "${PORT:-9000}:9000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always
